<h1><%= @membership.name %>'s Wishlist</h1>

<div class="menu">
  <%= link_to "« Back to group", group_path(@group) %>

  <%= link_to "Edit wishlist", edit_group_membership_path(@group, @membership) %>
</div>

<div class="box">
  <p>
    <% if @membership.wishlist.present? %>
      <%= @membership.wishlist %>
    <% else %>
      <%= @membership.name %> doesn't have a wishlist yet.
    <% end %>
  </p>
</div>
